# Form submissions — defaults to FormSubmit if unset
VITE_FORM_ENDPOINT=https://formsubmit.co/Stay@hackfarm.co.nz

# Canonical site origin (no trailing slash). Preview default:
VITE_SITE_ORIGIN=https://agent5479.github.io
# After DNS cutover:
# VITE_SITE_ORIGIN=https://www.hackfarm.co.nz

# NIWA tides — server/CI only (never expose in VITE_*).
# GitHub Actions: add repository secret NIWA_API_KEY
# Local refresh: NIWA_API_KEY=... npm run tides
# NIWA_API_KEY=
